<div class="container">
	<h1>Create a post</h1>
	<%= form_for @post, url: {action: "create"} do |f| %>
		<%= f.text_field :title, placeholder: "Title" %>
		<%= f.text_area :content, placeholder: "Blog here" %>
		<%= f.submit "Create" %>
	<%end%>
</div>